{
  "name": "mytransport-firebase-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-mytransport --config ../../firebase.json \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.4",
    "firebase": "10.14.1",
    "firebase-tools": "15.30.0"
  },
  "overrides": {
    "undici": "^6.28.0"
  }
}
