@inject NavigationManager Navigation
@inject MyTransportAppWASM.Services.AppStateService AppState

@code {
    protected override void OnInitialized()
    {
        AppState.ReturnUrl = Navigation.Uri;
        AppState.RequestShowLogin();
    }
}
